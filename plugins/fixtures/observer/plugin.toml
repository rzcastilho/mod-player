identifier  = "org.modplayer.fixture.observer"
name        = "Observer fixture"
description = "Holds only playback.observe and calls a transport.control-only request, to exercise permission_denied."
version     = "1.0.0"
api         = "1.0"
author      = "ModPlayer project"
license     = "MIT OR Apache-2.0"
source      = "fixture"
entry       = "main.luau"

[[permissions.required]]
permission    = "playback.observe"
justification = "Needs ready_ack to trigger a same-tick transport.seek(0) refusal (US2 T081, spec.md acceptance 1)."
